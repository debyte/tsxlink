const from = (...rows: string[]) => rows.join("\n");

export const HEADER = from(
  ",=0=0=0=0=(__    T  S  X    L  I  N  K    __)=0=0=0=0='",
);

export const DESC = from(
  "Link components from the popular design tools to presentation TSX in React.",
  "See more at: https://github.com/debyte/tsxlink",
);
